## 0.1.0

* Initial release of `segmented_progress_indicator`.
* `SegmentedProgressIndicator` with optional `centerValue`.
* `FadingSegmentedSpinner` with child overlay, `visibleTrail`, and `showHeadDot`.
* Example app, widget tests, and documentation assets.
